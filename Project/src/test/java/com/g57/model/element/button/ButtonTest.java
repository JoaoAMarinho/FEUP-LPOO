package com.g57.model.element.button;

import com.g57.model.Position;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import java.io.IOException;

import static org.junit.jupiter.api.Assertions.*;

class ButtonTest {
    Button button;
/*
    @BeforeEach
    void setUp() throws IOException {
        this.button = new MenuButton(new Position(4, 4), "Buttontest", null);
    }

    @Test
    void getWidth() {
        assertEquals("Buttontest".length(),button.getWidth());
    }

    @Test
    void getHeight() {
        assertEquals(1,button.getHeight());
    }

    @Test
    void getText() {
        assertEquals("Buttontest",button.getText());
    }

    @Test
    void isActive() {
        button.setColor("#FFFF00");
        assertTrue(button.isHighlighted());
        button.setColor("#FFFFFF");
        assertFalse(button.isHighlighted());
    }*/

}